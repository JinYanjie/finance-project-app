<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/extensionColor" />
    <gradient android:startColor="@color/extensionColor"
        android:endColor="@color/extensionOtherColor"
        android:angle="0"/>
    <stroke
        android:width="1px"
        android:color="@color/divider" />
    <corners android:radius="10dp" />
</shape>